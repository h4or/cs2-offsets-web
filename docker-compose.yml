services:
  app:
    image: node:latest
    container_name: cs2_offsets_app

    working_dir: /app

    volumes:
      - .:/app
      - /app/node_modules

    command: sh -c "npm install && npm run run"

    ports:
      - "1337:1337"
